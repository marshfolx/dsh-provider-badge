/**
 * dsh-provider-badge — browser half entry types.
 * The client plugin registers a provider badge into the
 * `conversation.input.right` slot via the `slots` service.
 */
export declare const name: 'provider-badge';
export declare const inject: readonly ['slots'];
export declare function apply(ctx: unknown): void;